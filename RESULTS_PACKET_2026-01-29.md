# Results Packet (for handoff) — 2026‑01‑29

This is a compact, copy/paste-friendly packet you can give another GPT.

## Ground rules / invariants

- Phase‑3B legal: measurement spine frozen (DN‑map/logdet proxy + label‑scramble null + empirical p-values; reject at α=0.01).
- Mode: `dnmap_only` (no closure-channel contamination).

## Completed runs + where to look

### v2 (scan+confirm)

- Scan root: `out_phase3B_preregistered_uniqueness_64_127_v2`
  - prereg digest: `4f19febe1d28f72f` (see `preregistration_digest.txt`)
  - KPI table: `kpi_fraction.csv`
- Confirm root: `out_phase3B_confirm_preregistered_uniqueness_rejecting_seeds_N16384_v2`

Headline KPI (from `kpi_fraction.csv`):
- `legacy`: 0/64 rejecting seeds (both windows)
- `geom_v4_theta0p125_autotune`: 1/64 rejecting seeds in primary (2.0,5.0); 0/64 in secondary (0.6,7.5)

### v5 (scan+confirm)

- Scan root: `out_phase3B_preregistered_uniqueness_64_127_v5`
  - prereg digest: `da3663ff6d51c570`
  - KPI table: `kpi_fraction.csv`
- Confirm root used for the v5 scan hit followup (actually v4 hit inside the v5 scan):
  - `out_phase3B_confirm_preregistered_uniqueness_rejecting_seeds_N16384_v5scan/raw.csv`

Headline KPI (from `kpi_fraction.csv`):
- `legacy`: 0/64 rejecting seeds (both windows)
- `geom_v5_theta0p125_autotune`: 0/64 rejecting seeds (both windows)
- `geom_v4_theta0p125_autotune`: 1/64 rejecting seeds in primary (2.0,5.0); 0/64 in secondary (0.6,7.5)

Confirm for the scan-selected v4 seed:
- Seed 110 (primary window) rejected for 1/7 anchors (anchor 46). Reject-rate 1/7 ≈ 0.1429.

### v5 Sweep A (scan only)

- Scan root: `out_phase3B_preregistered_uniqueness_64_127_v5_sweepA`
  - prereg digest: `beaab7e48e776b21`
  - KPI table: `kpi_fraction.csv`
  - `uniqueness.csv` and `digest_invariance_summary.csv` exist

Sweep family:
- `legacy` + 6 v5 variants sweeping `geom_v5_harmonic_gamma ∈ {0.5,1.0}` and `geom_v5_harmonic_strength ∈ {0.5,1.0,2.0}`, with `eps_auto_tune` targeting ΔTp≈0.02.

Headline KPI:
- No rejecting seeds in either window for any sweep backend (0/64 across all).

## Interpretation snapshot

- v5 baseline (theta0.125 + autotune) produced no scan hits (0/64).
- v5 Sweep A also produced no scan hits across the preregistered harmonic grid.
- v4 autotune continues to show rare scan hits (≈1/64) but confirm is not robust across anchors (1/7 anchors on seed 110).

## Suggested next step (if continuing Phase‑3B legal)

- Run a preregistered Sweep B that expands v5’s admissible knob set (e.g., include stronger harmonic strengths and/or a second theta_scale), still with eps auto-tune pinned to ΔTp≈0.02, and then confirm only scan-selected primary-window hits.

## v6 Phase backend (scan+confirm)

- Scan root: `out_phase3B_preregistered_uniqueness_64_127_v6_phase`
  - raw: `raw.csv`
  - per-seed KPI: `kpi_by_seed.csv`
  - per-seed windowpair KPI: `kpi_by_seed_windowpair.csv`
  - rankings: `seed_rankings.csv`
  - windowpair summary reports (generated by `tools/summarize_windowpair_narrow_wide.py --report-dir`):
    - `windowpair_top_primary.csv`
    - `windowpair_top_narrow_only.csv`
    - `windowpair_top_wide_only.csv`
    - `windowpair_best_per_backend_primary.csv`
- Confirm root (for scan-selected hits): `out_phase3B_confirm_preregistered_uniqueness_rejecting_seeds_N16384_v6scan`

Headline KPI (from scan + windowpair summaries):

- `geom_v6_theta0p125_l0.05_c2.0_autotune`: no robust scan hits across anchors in the primary window; best observed wide-only behavior is secondary 1/7 anchors (seed 91).
- `geom_v4_theta0p125_autotune`: the only scan-gated candidate remains seed 110 with primary 3/7 anchors rejecting and secondary 0/7.

Confirm for scan-selected v4 seed (inside the v6 scan pack):

- Seed 110: primary window rejected for 1/7 anchors (anchor 46); secondary window 0/7. Does not meet prereg robustness target ≥5/7.

## Latest GPT feedback (verbatim, 2026-01-29)

Below is external feedback pasted into the session; captured here for provenance.

1. Executive interpretation (short, honest, correct)

Bottom line:
Your pipeline is now clean, preregistered, audit-safe, and behaving exactly as a serious RH-mechanism testbed should. The v6 geometry did not produce a robust DN-map rigidity signal under preregistered criteria, and that is not a failure—it is a useful negative result that sharply constrains the admissible mechanism class.

There are no red flags in the plumbing or logic. The results are internally consistent, stable under confirm, and the null behavior is sane.

What is happening is that:

DN-map rigidity does occur, but

it appears seed-conditional and anchor-localized, not global, and

attempts to “spread” it (v4 → v5 → v6) by making the self-dual structure more explicit weaken the effect instead of strengthening it.

That is a very strong diagnostic signal.

2. What the v6 results are really telling you (conceptually)
2.1 Phase-3B goal reminder (important)

Phase-3B is not “prove RH”.
It is:

Test whether a Tp-only geometric deformation can induce DN-map spectral rigidity at pqM=1 under a frozen measurement spine.

By that standard, your conclusions are now sharp.

2.2 Why v6 failed (and why that’s informative)

... (full paste retained in chat; this packet keeps the key claim above and the concrete artifact links)

## Notes / next fork

- The data pattern to preserve: any “signal” so far is window-conditional (primary 2–5) and anchor-localized.
- If staying Phase‑3B: preregister a localized-defect admissible subclass (self-adjoint + bounded ΔTp band) rather than further smoothing/globalizing.
- If moving to Phase‑3C: formalize a completion identity for the observable (what is being approximated, why entire, and what functional-equation symmetry is enforced).
